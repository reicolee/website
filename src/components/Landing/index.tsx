import React from "react"
import PropTypes from "prop-types"

import "./style.scss"

const Landing = ({ data }) => {
  return <div></div>
}

Landing.propTypes = {
  data: PropTypes.object,
}

export default Landing
